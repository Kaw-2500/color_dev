using Unity.VisualScripting;
using UnityEngine;

public class HitDamage :MonoBehaviour
{
    public float HitAttackDamage(float damage)
    {
        // ここにダメージ計算のロジックを実装
        // 今はそのままの値を返す
        return damage;
    }
}